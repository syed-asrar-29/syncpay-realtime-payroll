## Packages
framer-motion | Smooth page transitions and UI animations
lucide-react | Beautiful icons for the interface
date-fns | Formatting dates for salary and leave records
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
WebSocket connects to window.location.protocol + '//' + window.location.host + '/ws'
